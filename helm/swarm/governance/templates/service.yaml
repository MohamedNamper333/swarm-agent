apiVersion: v1
kind: Service
metadata:
  name: {{ include "governance.fullname" . }}
  labels:
    {{- include "governance.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.targetPort }}
      protocol: TCP
      name: http
  selector:
    {{- include "governance.selectorLabels" . | nindent 4 }}
