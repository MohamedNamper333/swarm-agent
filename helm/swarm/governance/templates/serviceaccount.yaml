apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "governance.serviceAccountName" . }}
  labels:
    {{- include "governance.labels" . | nindent 4 }}
{{- with .Values.imagePullSecrets }}
imagePullSecrets:
  {{- toYaml . | nindent 2 }}
{{- end }}
