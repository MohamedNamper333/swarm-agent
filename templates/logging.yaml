name: logging
version: "1.0.0"
author: swarm-core
description: Logs task lifecycle events (start, complete, error)
enabled: true
priority: 10
module_path: swarm.plugins.builtin.logging_plugin
class_name: LoggingPlugin
config:
  log_level: INFO
