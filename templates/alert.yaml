name: alert
version: "1.0.0"
author: swarm-core
description: Triggers alerts on task errors
enabled: true
priority: 90
module_path: swarm.plugins.builtin.alert_plugin
class_name: AlertPlugin
config:
  max_alerts: 1000
