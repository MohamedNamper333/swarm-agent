name: rate-limiter
version: "1.0.0"
author: swarm-core
description: Rate-limits API calls using token bucket algorithm
enabled: true
priority: 30
module_path: swarm.resilience.rate_limiter
class_name: RateLimiter
config:
  max_tokens: 100
  refill_rate: 10.0
