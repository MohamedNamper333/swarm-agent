name: auth
version: "1.0.0"
author: swarm-core
description: Authentication and authorization plugin for API access control
enabled: true
priority: 5
module_path: swarm.plugins.builtin.auth_plugin
class_name: AuthPlugin
config:
  auth_type: jwt
  token_expiry: 3600
  require_auth: true
