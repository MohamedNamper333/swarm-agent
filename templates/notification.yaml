name: notification
version: "1.0.0"
author: swarm-core
description: Notification plugin for task events and alerts
enabled: true
priority: 30
module_path: swarm.plugins.builtin.notification_plugin
class_name: NotificationPlugin
config:
  channels:
    - console
  notify_on:
    - error
    - complete
