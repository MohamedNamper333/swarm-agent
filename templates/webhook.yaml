name: webhook
version: "1.0.0"
author: swarm-core
description: Webhook plugin for external integrations
enabled: false
priority: 40
module_path: swarm.plugins.builtin.webhook_plugin
class_name: WebhookPlugin
config:
  endpoints: []
  timeout: 30
  retry_count: 3
